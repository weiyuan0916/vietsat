{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
      {
        "source": "/((?!assets|favicon.ico|manifest.json|service-worker.js).*)",
        "destination": "/"
      }
    ]
  }