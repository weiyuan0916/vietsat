{
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
      {
        "source": "/((?!assets|icons|favicon.ico|manifest.json|manifest.webmanifest|service-worker.js).*)",
        "destination": "/"
      }
    ]
  }